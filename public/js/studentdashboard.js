// Add this JavaScript to your existing dashboard.js or in a <script> block
document.querySelectorAll('.details-btn').forEach(button => {
    button.addEventListener('click', () => {
        alert('Job details coming soon...');
    });
});
